#include <stdio.h>

int main() {
    // Variables should be declared before using them.
    int A, B, C, D;

    // Assigning values to variables.
    A = 22;
    B = A;
    C = A + B;

    // Valid statements:
    printf("A = %d\n", A);
    printf("B = %d\n", B);
    printf("C = %d\n", C);

    /*
    Wrong statements:

    A + B = C;   // Expression cannot be assigned a value.
    A = B = C;   // This is actually valid in C, but it assigns
                 // the value of C to both B and A.

    Variables can store values and can also be used in expressions.

    To check whether a number is divisible by another number,
    take both numbers from the user and use the modulus (%) operator.

    Example:
    If (num % divisor == 0)
        The number is divisible.
    */

    return 0;
}
